import styled from "styled-components"
import { neutralGray } from "../../Constants/colors"

export const VolunteerCardContainer = styled.section`
    display:flex;
    flex-direction: column;
    border: 1px dotted ${neutralGray};
    border-radius: 30px;
    width: 20vw;
    padding: 8px 16px;
`